// Cấu hình 1 chỗ duy nhất cho base URL của backend API.
// Đổi ở đây khi deploy (vd sang domain/production).
// Có thể để '' nếu frontend được phục vụ cùng origin với API (server/express static).
window.API_BASE = 'http://localhost:3000';
